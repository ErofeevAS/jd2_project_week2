database.url=jdbc:h2:~/test
database.username=sa
database.driver=org.h2.Driver
database.create.documents.table.query = CREATE TABLE IF NOT EXISTS documents( id BIGINT auto_increment PRIMARY KEY,  unique_number VARCHAR(255) NOT NULL UNIQUE, description VARCHAR(255), deleted BOOLEAN DEFAULT FALSE NOT NULL);
